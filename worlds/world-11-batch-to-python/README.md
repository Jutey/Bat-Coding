# WORLD 11 — Batch → Python
## The Bridge World

---

### Grand Quest Context

The network is restored.  
All 10 sectors online. The Cascade Protocol contained.

A.E.G.I.S. delivers its final briefing:

> "You rebuilt the world's computer network with a language most people have never heard of.  
> But Batch isn't the destination. It's the foundation.  
>
> Everything you built here — the variables, the logic, the loops, the functions, the files —  
> all of it exists in every programming language ever written.  
>
> Python. JavaScript. C. Rust. Go.  
> They all do the same things. They just look different.  
>
> You're not learning Python now.  
> You're recognizing it."

---

### The Big Idea

The student doesn't realize until this world how much they already know.

This world makes it explicit:

| Batch | Python |
|-------|--------|
| `echo Hello` | `print("Hello")` |
| `set name=Alex` | `name = "Alex"` |
| `echo %name%` | `print(name)` |
| `set /p name=Enter name: ` | `name = input("Enter name: ")` |
| `if "%x%"=="y"` | `if x == "y":` |
| `goto start` | `while True:` |
| `call :func` | `def func():` |
| `echo > file.txt` | `open("file.txt","w")` |

Same ideas. Different syntax.

---

### Lessons

| # | Lesson | The Comparison |
|---|--------|----------------|
| 101 | Batch vs Python | conceptual mapping — you've outgrown Batch |
| 102 | Variables in Python | `set x=5` → `x = 5` |
| 103 | Input in Python | `set /p` → `input()` |
| 104 | Loops in Python | `goto` → `while` / `for` |
| 105 | Functions in Python | `call :func` → `def func():` |
| 106 | Files in Python | echo redirect → `open()`/`.write()` |
| 107 | Running Python from Batch | calling `python script.py` from a `.bat` |
| 108 | Combining Languages | using Batch and Python together |
| 109 | Final Project | the capstone project |
| 110 | Graduation Ceremony | Terminal Prime: fully restored |

---

### Lesson 109 — The Capstone Project

This lesson has no new commands. It has no code to run.

It's a single page.

The student reads:

> You just wrote a working Python program.
>
> You learned it in one world.
>
> That's because you already knew programming.  
> You learned it in Batch.
>
> Every variable, every if statement, every loop, every function —  
> you built all of that from scratch, in a language most programmers have never touched.
>
> Python was just translation.
>
> Any language you pick up next will be the same.  
> JavaScript. Swift. Rust. Go.  
> Different words. Same ideas.
>
> You know how to program now.  
> You have since World 4.

---

### Lesson 110 — Graduation Ceremony

A.E.G.I.S. delivers the final message:

> "The network is online. You did that.
>
> What do you build next?
>
> Python has libraries for games, websites, data, music, AI.  
> JavaScript builds the web.  
> Swift builds iPhone apps.  
> Unity uses C# for games.
>
> Pick one. The door is open.  
> You already know how to walk through it.
>
> Good work, Cadet.  
>
> — A.E.G.I.S."

---

### Final Achievement

**CENTURION** — Complete all 110 lessons.

> "You rebuilt the world. Sector by sector. Command by command.  
> From `echo Hello` to a working Python program.  
> You're a programmer."

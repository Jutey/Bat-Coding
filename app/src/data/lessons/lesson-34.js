// Lesson 34 — Countdown Timers
export default {
  id: 'world-04/lesson-34-countdown-timers',
  title: 'Countdown Timers',
  world: 'SECTOR 4 — THE LOOP ENGINE',
  xp: 100,
  steps: [
    {
      type: 'learn',
      title: 'Counting Down',
      body: 'A countdown is just a loop that decrements (subtracts from) a counter instead of incrementing.\n\nStart at 10. Subtract 1. Print. Repeat until 0.',
      code: null,
    },
    {
      type: 'learn',
      title: 'The Countdown Pattern',
      body: 'Start high. Subtract 1 each loop. Stop when counter reaches 0.',
      code: '@echo off\nset count=5\n:countdown\necho %count%...\nset /a count=count-1\nif %count% == 0 goto launch\ngoto countdown\n:launch\necho LAUNCH!',
      output: '5...\n4...\n3...\n2...\n1...\nLAUNCH!',
    },
    {
      type: 'learn',
      title: 'timeout — Real Time Delay',
      body: '`timeout /t N /nobreak` pauses the program for N seconds. Perfect for countdown timers that actually count in real time.',
      code: '@echo off\nset count=3\n:countdown\necho %count%\ntimeout /t 1 /nobreak > nul\nset /a count=count-1\nif %count% GTR 0 goto countdown\necho GO!',
      output: '3\n(1 second)\n2\n(1 second)\n1\n(1 second)\nGO!',
    },
    {
      type: 'predict',
      question: 'What does `> nul` do after the timeout command?',
      code: 'timeout /t 1 /nobreak > nul',
      options: [
        'It crashes the timeout',
        'It redirects timeout\'s output to nul (nowhere) so the countdown message doesn\'t appear',
        'It creates a file called nul',
        'It makes the timeout longer',
      ],
      correct: 1,
      explanation: 'timeout normally prints "Waiting for X seconds...". Redirecting to `nul` (the null device) silences that message, keeping only your countdown numbers visible.',
    },
    {
      type: 'predict',
      question: 'What does this countdown print?',
      code: '@echo off\nset t=3\n:loop\necho T-%t%\nset /a t=t-1\nif %t% GTR 0 goto loop\necho BLAST OFF',
      options: ['T-3\nT-2\nT-1\nBLAST OFF', 'T-3\nBLAST OFF', '3\n2\n1\nBLAST OFF', 'T-0\nBLAST OFF'],
      correct: 0,
      explanation: 'It prints T-3, T-2, T-1 (t goes 3→2→1, exits when t is not GTR 0), then BLAST OFF.',
    },
    {
      type: 'fill',
      prompt: 'Complete the countdown — subtract 1 from the counter each loop:',
      template: '@echo off\nset n=10\n:loop\necho %n%\nset ___ n=n-1\nif %n% GTR 0 goto loop\necho DONE',
      blank: '___',
      answer: '/a',
      hint: 'The flag that enables arithmetic in set.',
    },
    {
      type: 'fix',
      prompt: 'The countdown goes up instead of down. Fix it.',
      code: '@echo off\nset count=5\n:loop\necho %count%\nset /a count=count+1\nif %count% == 0 goto done\ngoto loop\n:done\necho Finished',
      answer: '@echo off\nset count=5\n:loop\necho %count%\nset /a count=count-1\nif %count% == 0 goto done\ngoto loop\n:done\necho Finished',
      bugHint: 'Subtracting should use minus (-), not plus (+).',
      bugType: 'wrong_operator',
    },
    {
      type: 'build',
      prompt: 'Build a bomb defusal countdown.\n\nCount down from 30 (or use timeout for real seconds if you want). At 10 seconds remaining, show "WARNING: 10 SECONDS LEFT!" At 0, show "BOOM!" or "DEFUSED!" based on a hasWire variable.',
      starterCode: '@echo off\ncolor 0C\ntitle BOMB DEFUSAL\nset count=10\nset defused=NO\ncls\necho === BOMB DEFUSAL SYSTEM ===\necho.',
      minLines: 15,
      achievement: 'countdown_king',
    },
    {
      type: 'reward',
      xp: 100,
      achievement: 'countdown_king',
      message: 'The Loop Engine counts every second.\n\nTime is just another variable.',
      storyUpdate: 'TIMER MODULE LOADED — TEMPORAL LOOP ACTIVE',
    },
  ],
};

// Lesson 21 — If Statements
export default {
  id: 'world-03/lesson-21-if-statements',
  title: 'If Statements',
  world: 'SECTOR 3 — LOGIC CORE',
  xp: 100,
  steps: [
    {
      type: 'learn',
      title: 'The Logic Core',
      body: 'You\'ve entered Sector 3 — the Logic Core.\n\nThis sector makes **decisions**.\n\nEvery game, every program that responds to the player uses logic. The computer asks: is this true? If yes — do this. If no — do that.',
      code: null,
    },
    {
      type: 'learn',
      title: 'if — Making Decisions',
      body: 'The `if` command checks a condition. If it\'s true, it runs the command after it.',
      code: '@echo off\nset health=50\nif %health% == 0 echo You are dead!\necho Program continues.',
      output: 'Program continues.',
    },
    {
      type: 'learn',
      title: 'if/else with goto',
      body: 'For an if-else, use `goto` to jump to different sections of code.\n\n`:label` marks a section. `goto label` jumps to it.',
      code: '@echo off\nset health=0\nif %health% == 0 goto dead\ngoto alive\n:dead\necho You are dead!\ngoto end\n:alive\necho You are alive!\n:end',
      output: 'You are dead!',
    },
    {
      type: 'predict',
      question: 'What does this program print when health is 100?',
      code: '@echo off\nset health=100\nif %health% == 0 echo DEAD\nif %health% == 100 echo FULL HEALTH',
      options: ['DEAD', 'FULL HEALTH', 'DEAD\nFULL HEALTH', 'Nothing'],
      correct: 1,
      explanation: 'First `if` checks if health==0. It\'s not (it\'s 100), so nothing. Second `if` checks if health==100. That\'s true, so it prints "FULL HEALTH".',
    },
    {
      type: 'predict',
      question: 'In an if/goto structure, what does the `:end` label do?',
      code: '@echo off\nset x=5\nif %x% == 5 goto yes\ngoto end\n:yes\necho Five!\n:end',
      options: [
        'It crashes the program',
        'It\'s a target that both branches jump to — it marks the end of the if/else block',
        'It prints "end" to the screen',
        'Nothing — labels are invisible',
      ],
      correct: 1,
      explanation: '`:end` is a label — a named location in the code. `goto end` jumps past the `:yes` block so code only runs in one branch.',
    },
    {
      type: 'fill',
      prompt: 'Complete the condition check:',
      template: '@echo off\nset level=10\nif %level% __ 10 echo MAX LEVEL!',
      blank: '__',
      answer: '==',
      hint: 'Two equals signs test for equality in Batch.',
    },
    {
      type: 'fix',
      prompt: 'The if statement never fires. Fix the bug.',
      code: '@echo off\nset score=100\nif score == 100 echo High score!',
      answer: '@echo off\nset score=100\nif %score% == 100 echo High score!',
      bugHint: 'Variable names need percent signs when reading them. "score" is just text — "%score%" is the value.',
      bugType: 'missing_syntax',
    },
    {
      type: 'build',
      prompt: 'Build a "checkpoint system".\n\nSet a password variable. Ask the user for a password. If they match, print "ACCESS GRANTED". Use goto for the if/else logic.',
      starterCode: '@echo off\nset correctPassword=bat123\nset /p guess=Enter password: \nif %guess% == %correctPassword% goto granted\ngoto denied\n:denied\necho ACCESS DENIED\ngoto end\n:granted\n',
      minLines: 8,
      achievement: 'logical',
    },
    {
      type: 'reward',
      xp: 100,
      achievement: 'logical',
      message: 'The Logic Core fires for the first time.\n\nDecisions can be made. Paths can diverge.',
      storyUpdate: 'LOGIC NODE 1 ACTIVE — DECISION TREE INITIALIZED',
    },
  ],
};

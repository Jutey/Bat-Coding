// Lesson 29 — NOT and Combining Conditions
export default {
  id: 'world-03/lesson-29-not-and-combining-conditions',
  title: 'NOT and Combining Conditions',
  world: 'SECTOR 3 — LOGIC CORE',
  xp: 100,
  steps: [
    {
      type: 'learn',
      title: 'The Opposite',
      body: 'Sometimes you don\'t want to say "if X is true, do Y".\n\nYou want to say "if X is NOT true, do Y".\n\nBatch has a `not` keyword for exactly this.',
      code: null,
    },
    {
      type: 'learn',
      title: 'if not — The Inverse Check',
      body: '`if not` reverses the condition. If the condition is FALSE, the action runs.',
      code: '@echo off\nset hasKey=NO\nif not %hasKey% == YES echo You need a key!\nif %hasKey% == YES echo You can pass.',
      output: 'You need a key!',
    },
    {
      type: 'learn',
      title: 'Checking if a File Exists',
      body: '`if exist filename` checks if a file exists. `if not exist filename` checks if it does NOT exist.',
      code: '@echo off\nif not exist save.txt echo No save file found.\nif exist save.txt echo Save file loaded.',
      output: 'No save file found.',
    },
    {
      type: 'predict',
      question: 'What does this program print when shield=NO?',
      code: '@echo off\nset shield=NO\nif not %shield% == YES echo No shield equipped!\nif %shield% == YES echo Shield: active',
      options: [
        'No shield equipped!',
        'Shield: active',
        'Both lines',
        'Nothing',
      ],
      correct: 0,
      explanation: '`if not %shield% == YES` — shield is NO, so the condition (shield==YES) is FALSE, so NOT makes it TRUE → prints "No shield equipped!"',
    },
    {
      type: 'predict',
      question: 'What does `if not exist savegame.txt goto newgame` do?',
      code: 'if not exist savegame.txt goto newgame',
      options: [
        'Jumps to :newgame if savegame.txt EXISTS',
        'Jumps to :newgame if savegame.txt does NOT exist',
        'Creates savegame.txt',
        'Deletes savegame.txt',
      ],
      correct: 1,
      explanation: '`if not exist` — if the file does NOT exist, the condition is true, so it jumps to :newgame. Useful for detecting first-time players.',
    },
    {
      type: 'fill',
      prompt: 'Complete the check — if the player does NOT have enough gold:',
      template: '@echo off\nset gold=30\nset price=50\nif ___ %gold% GEQ %price% echo Not enough gold!',
      blank: '___',
      answer: 'not',
      hint: 'What keyword reverses a condition in Batch?',
    },
    {
      type: 'fix',
      prompt: 'The NOT logic is backwards — fix the condition.',
      code: '@echo off\nset alive=YES\nif not %alive% == YES echo Still in the fight!\nif not %alive% == YES goto gameover',
      answer: '@echo off\nset alive=YES\nif %alive% == YES echo Still in the fight!\nif not %alive% == YES goto gameover',
      bugHint: 'The first line should print when alive IS YES. The second line should run when alive is NOT YES.',
      bugType: 'inverted_logic',
    },
    {
      type: 'build',
      prompt: 'Build a game save/load detector.\n\nUse `if exist` and `if not exist` to check for a save file. If it exists, display a "Load Game" option. If it doesn\'t, display a "New Game" message and explain there\'s no save.',
      starterCode: '@echo off\ncolor 0A\ntitle SAVE DETECTOR\ncls\necho === STARTUP ===\necho.\n',
      minLines: 10,
      achievement: 'negator',
    },
    {
      type: 'reward',
      xp: 100,
      achievement: 'negator',
      message: 'The Logic Core sees both sides now.\n\nTrue and false. Present and absent.',
      storyUpdate: 'INVERSE LOGIC ONLINE — NOT OPERATOR VERIFIED',
    },
  ],
};
